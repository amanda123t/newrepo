# ============================================================
# DATABASE — escolha uma das opções:
# ============================================================

# DESENVOLVIMENTO (SQLite — sem instalação necessária, padrão automático)
DATABASE_URL="file:./dev.db"

# PRODUÇÃO (PostgreSQL — ex: Neon, Supabase, Vercel Postgres)
# DATABASE_URL="postgresql://user:password@host:5432/processflow?sslmode=require"
